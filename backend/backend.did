service : {
  clearConversationHistory: () -> ();
  generateFallbackResponse: (text) -> (text);
  getConversationHistory: () -> (vec record {
                                       text;
                                       text;
                                     }) query;
  sendMessage: (text, text) -> ();
}
